# azure-default-pvc.yaml
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-default-disk
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: default
  resources:
    requests:
      storage: 5Gi
